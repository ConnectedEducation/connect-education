<div class="full flex">
    <div class="full panel">
        <h1>{{courseTitle}}</h1>
        <hr>
        <p>
            {{ description }}
        </p>
        <hr>
        <div>
            <!-- loop over participants array and render participants here -->
        </div>
    </div>
</div>